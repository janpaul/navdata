type Config = {
  writeJsonFile: boolean
  insertWays: boolean
  trafficFlowEnabled: boolean
  taxiRoutingEnabled: boolean
}

const config: Config = {
  writeJsonFile: true,
  insertWays: false,
  trafficFlowEnabled: true,
  taxiRoutingEnabled: true,
}
export default config
